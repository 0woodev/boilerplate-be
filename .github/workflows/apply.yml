name: Terraform Apply

on:
  push:
    branches: [dev, main]
    paths:
      - 'terraform/**'
      - 'app/**'
      - 'common/**'
      - 'requirements.txt'

permissions:
  id-token: write
  contents: read

jobs:
  apply:
    runs-on: ubuntu-latest
    # dev 브랜치 → dev 환경 변수, main → prod 환경 변수 자동 참조
    environment: ${{ github.ref_name == 'main' && 'prod' || github.ref_name }}
    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
      PROJECT_NAME: ${{ vars.PROJECT_NAME }}
      STAGE: ${{ github.ref_name == 'main' && 'prod' || github.ref_name }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ vars.AWS_ACCOUNT_ID }}:role/${{ env.PROJECT_NAME }}-${{ env.STAGE }}-github-actions
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 키에 github.sha 박지 않음 — incremental Makefile 이 .build/ 안에서 SHA256 비교로
      # endpoint 별 재빌드 판단하니, 캐시는 코드/의존성 내용 기반으로 재사용해도 안전.
      # 같은 키면 cache action 이 save 를 자동 skip → 중복 저장 방지.
      - name: Cache build artifacts
        uses: actions/cache@v4
        with:
          path: .build/
          key: build-${{ env.STAGE }}-${{ hashFiles('requirements.txt', 'app/**', 'common/**', 'Makefile') }}
          restore-keys: |
            build-${{ env.STAGE }}-

      - name: Build (Lambda zips + Layers)
        run: make all

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Init
        working-directory: terraform
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.TF_STATE_BUCKET }}" \
            -backend-config="key=${{ vars.PROJECT_NAME }}/${{ env.STAGE }}/terraform.tfstate" \
            -backend-config="region=${{ vars.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ vars.PROJECT_NAME }}-tf-lock"

      - name: Terraform Apply
        working-directory: terraform
        run: |
          set -o pipefail
          terraform apply -auto-approve -input=false \
            -var="stage=${{ env.STAGE }}" \
            -var="project_name=${{ vars.PROJECT_NAME }}" \
            -var="aws_region=${{ vars.AWS_REGION }}" \
            -var="aws_account_id=${{ vars.AWS_ACCOUNT_ID }}" \
            -var="github_owner=${{ vars.GH_OWNER }}" \
            -var="tf_state_bucket=${{ vars.TF_STATE_BUCKET }}" \
            -var="fe_domain=${{ vars.FE_DOMAIN }}" \
            -var="domain=${{ vars.DOMAIN }}" \
            -var="be_domain=${{ vars.BE_DOMAIN }}"
